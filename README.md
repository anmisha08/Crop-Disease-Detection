# Crop-Disease-Detection
This project uses Convolutional Neural Networks (CNN) in Python to automatically detect and classify crop diseases from leaf images. The model analyzes visual patterns in leaves and provides accurate predictions, making it useful for smart agriculture applications.

